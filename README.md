# Fox Group ERP System

نظام إدارة موارد المؤسسة (ERP) متكامل لإدارة المبيعات، المشتريات، المخزون، والخزينة.

## المميزات الرئيسية

- 🛒 **نقطة البيع (POS)**: واجهة سريعة وسهلة لإتمام المبيعات
- 📦 **إدارة المخزون**: تتبع المنتجات والكميات مع تنبيهات المخزون المنخفض
- 👥 **إدارة العملاء والموردين**: متابعة الحسابات والديون
- 💰 **إدارة الخزينة**: تتبع المصروفات والإيرادات
- 📊 **التقارير والتحليلات**: تقارير مفصلة عن المبيعات والأرباح
- 🔐 **نظام صلاحيات**: أدوار متعددة (مدير، محاسب، كاشير، أمين مخزن)
- 📱 **دعم وضع عدم الاتصال**: العمل بدون إنترنت مع المزامنة التلقائية
- 🌐 **واجهة عربية**: دعم كامل للغة العربية

## البنية التقنية

### Backend
- **Framework**: Django 5.0 + Django REST Framework
- **Database**: PostgreSQL
- **Authentication**: JWT (Simple JWT)
- **API Documentation**: Swagger/OpenAPI

### Frontend
- **Framework**: React 18 + TypeScript
- **Build Tool**: Vite
- **Styling**: Tailwind CSS
- **Icons**: Lucide React
- **State Management**: React Hooks

## المتطلبات

### Backend
- Python 3.10+
- PostgreSQL 14+
- pip

### Frontend
- Node.js 18+
- npm or yarn

## التثبيت والإعداد

### 1. إعداد Backend

```bash
# الانتقال إلى مجلد المشروع
cd fox_pos_project

# إنشاء بيئة افتراضية
python -m venv venv

# تفعيل البيئة الافتراضية
# Windows
venv\Scripts\activate
# Linux/Mac
source venv/bin/activate

# تثبيت المتطلبات
pip install -r requirements.txt

# إعداد قاعدة البيانات
python manage.py migrate

# تحميل البيانات الأولية
python manage.py load_initial_data

# إنشاء مستخدمين تجريبيين (اختياري)
python manage.py create_test_users

# تشغيل الخادم
python manage.py runserver
```

الخادم سيعمل على: `http://localhost:8000`

### 2. إعداد Frontend

```bash
# الانتقال إلى مجلد Frontend
cd fox-group-erp

# تثبيت المتطلبات
npm install

# إنشاء ملف البيئة
cp .env.example .env.local

# تحديث ملف .env.local
# VITE_API_URL=http://localhost:8000/api

# تشغيل خادم التطوير
npm run dev
```

التطبيق سيعمل على: `http://localhost:5173`

## المستخدمون الافتراضيون

بعد تشغيل `load_initial_data`:

| اسم المستخدم | كلمة المرور | الدور |
|--------------|-------------|-------|
| admin | admin123 | مدير |
| cashier | cashier123 | كاشير |

## البيانات الأولية

يتم تحميل البيانات التالية تلقائياً:
- 10 منتجات تجريبية
- 5 عملاء
- 3 موردين
- إعدادات النظام الافتراضية

## الاستخدام

### تسجيل الدخول
1. افتح المتصفح على `http://localhost:5173`
2. استخدم أحد المستخدمين الافتراضيين للدخول

### فتح وردية
1. انتقل إلى صفحة "نقطة البيع"
2. اضغط "فتح وردية"
3. أدخل رصيد البداية

### إتمام عملية بيع
1. اختر المنتجات من القائمة
2. حدد العميل (اختياري)
3. اختر طريقة الدفع
4. اضغط "إتمام البيع"

### إغلاق الوردية
1. اضغط "إغلاق الوردية"
2. أدخل الرصيد الفعلي
3. راجع تقرير Z-Report

## الوثائق

- [API Endpoints](docs/api_endpoints.md) - توثيق كامل لجميع نقاط النهاية
- [Integration Coverage](docs/integration_coverage.md) - تغطية التكامل بين Frontend و Backend
- [Integration Analysis](docs/integration_analysis.md) - تحليل تفصيلي للتكامل

## إدارة البيانات

### ⚠️ ملاحظات هامة

- البيانات التجريبية **لا يتم تحميلها تلقائياً** عند تشغيل `start.bat`
- لحذف البيانات بالكامل، يجب حذفها من **قاعدة البيانات** و **localStorage** معاً
- أمر `load_initial_data` يتحقق من وجود بيانات ولن يضيف بيانات مكررة

### تحميل البيانات التجريبية

```bash
cd fox_pos_project
python manage.py load_initial_data
```

### حذف جميع البيانات من قاعدة البيانات

```bash
cd fox_pos_project
python manage.py clear_all_data
```

### مسح ذاكرة التخزين المحلية (localStorage)

**الطريقة 1**: افتح المتصفح على `http://localhost:5173/clear-storage.html`

**الطريقة 2**: استخدم Console المتصفح (F12):
```javascript
localStorage.clear()
```

## الأوامر المفيدة

### Backend

```bash
# إنشاء مستخدم مدير جديد
python manage.py createsuperuser

# تشغيل الاختبارات
python manage.py test

# جمع الملفات الثابتة
python manage.py collectstatic
```

### Frontend

```bash
# بناء للإنتاج
npm run build

# معاينة البناء
npm run preview

# فحص الأكواد
npm run lint
```

## النشر

### Backend (Django)

```bash
# تحديث المتطلبات
pip freeze > requirements.txt

# جمع الملفات الثابتة
python manage.py collectstatic --noinput

# تشغيل مع Gunicorn
gunicorn fox_pos.wsgi:application --bind 0.0.0.0:8000
```

### Frontend (React)

```bash
# بناء للإنتاج
npm run build

# الملفات ستكون في مجلد dist/
# يمكن نشرها على أي خادم ويب (Nginx, Apache, etc.)
```

## المساهمة

نرحب بالمساهمات! يرجى:
1. عمل Fork للمشروع
2. إنشاء فرع للميزة الجديدة
3. عمل Commit للتغييرات
4. عمل Push للفرع
5. فتح Pull Request

## الترخيص

هذا المشروع مملوك لـ Fox Group ومطور بواسطة CairoCode.

## الدعم

للدعم الفني، يرجى التواصل مع فريق التطوير.

---

**Fox Group ERP v1.0.0**  
© 2025 Fox Group. All rights reserved.  
Developed by CairoCode
